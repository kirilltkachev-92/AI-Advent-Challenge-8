# День 24 — цитаты, источники и анти-галлюцинации.
# Эмбеддинги и поиск — локальная Ollama, агент/rewrite/реранкер/судья — DeepSeek.
#
# Подготовка: brew install ollama && ollama serve && ollama pull nomic-embed-text

# --- DeepSeek (обязателен) ---
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
# DEEPSEEK_MODEL=deepseek-chat

# --- Эмбеддинги (Ollama) ---
# OLLAMA_BASE_URL=http://localhost:11434
# EMBED_MODEL=nomic-embed-text

# --- Пороги (усиление дня: ниже порога ⇒ обязан сказать «не знаю») ---
# CANDIDATES_K=20       # top-K до фильтрации
# FINAL_K=4             # top-K после фильтрации
# SIM_THRESHOLD=0.55    # порог косинуса
# RERANK_THRESHOLD=6    # порог LLM-реранкера (0–10); не прошёл никто ⇒ «не знаю»

# --- Индекс ---
# RAG_STRATEGY=structure
# CHUNK_SIZE=1200
# CHUNK_OVERLAP=200

# --- Каталоги ---
# DOCS_DIR=docs
# OUTPUT_DIR=output
