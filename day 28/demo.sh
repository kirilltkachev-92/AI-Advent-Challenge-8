#!/usr/bin/env bash
# Демо для видео: RAG полностью локально + сравнение с облаком.
set -euo pipefail
cd "$(dirname "$0")"

echo "── 1. Всё локально: сервер и модели ─────────────────────────"
curl -s http://localhost:11434/api/version
echo
ollama list
echo

echo "── 2. Одиночный вопрос: retrieval + генерация локально ──────"
ASK="Что Алексей Гладков собирает у себя дома вместо Яндекс станции?" ./run.sh
echo

echo "── 3. Полное сравнение: локальная против облачной ───────────"
echo "   (8 вопросов × 3 прогона × 2 модели + слепой судья → output/report.md)"
./run.sh
