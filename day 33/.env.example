# День 33 — ассистент поддержки пользователей.

# Облачная генерация: DeepSeek. Ключ также ищется в ../day 25/.env,
# ../day 22/.env и ../day 17/.env — если он там уже есть, .env не нужен.
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_MODEL=deepseek-chat

# Эмбеддинги: локальная Ollama (ollama pull nomic-embed-text)
# OLLAMA_BASE_URL=http://localhost:11434
# EMBED_MODEL=nomic-embed-text

# MCP-сервер CRM (localhost)
# MCP_PORT=8033

# Сколько фрагментов базы знаний достаёт retrieval
# TOP_K=4
