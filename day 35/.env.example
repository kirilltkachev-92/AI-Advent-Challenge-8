# День 35 — AI-конвейер подготовки релиза.

# Облачная генерация: DeepSeek. Ключ также ищется в ../day 25/.env,
# ../day 22/.env и ../day 17/.env — если он там уже есть, .env не нужен.
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_MODEL=deepseek-chat

# Публикация: нужен авторизованный GitHub CLI (gh auth login), ключей тут нет.
