#!/usr/bin/env bash
# Демо для видео: оптимизация локальной LLM под конкретную задачу.
set -euo pipefail
cd "$(dirname "$0")"

echo "── 1. Всё локально: модели и кванты ─────────────────────────"
ollama list
echo

echo "── 2. Матрица оптимизации ───────────────────────────────────"
echo "   дефолты → параметры → prompt-шаблон → квант q3 → мини 0.5b"
echo "   (10 объявлений дня → JSON-карточки, оценка программная)"
./run.sh
echo

echo "── 3. Отчёт ─────────────────────────────────────────────────"
head -40 output/report.md
